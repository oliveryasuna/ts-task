{
  "extends": "./tsconfig.json",
  "include": [
    "../../global.d.ts",
    "./*.ts",
    "./scripts/**/*.ts"
  ],
  "compilerOptions": {
    //==================================================
    // Modules
    //==================================================

    "types": [
      "bun"
    ],

    //==================================================
    // Emit
    //==================================================

    "declaration": false,
    "declarationMap": false
  }
}
